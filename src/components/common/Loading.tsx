import React from 'react';

const Loading: React.FC = () => {
    return <div className="spinner border-blue-500 h-12 w-12 animate-spin rounded-full border-t-4 text-primary"></div>;
};

export default Loading;
